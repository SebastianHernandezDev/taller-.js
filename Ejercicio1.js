const prompt = require("prompt-sync")();


function calcularPropina(total,porcentaje){
  return totalConPropina =total + (total * porcentaje / 100);
  
}

let totall= calcularPropina(100,10);
console.log(totall);



